#include<stdio.h>

int main(){
	int n;
	printf("hay nhap 1 so (khac0): ");
	scanf("%d",&n);
	if (n > 0){
		printf("%d là so duong",n);
	}
	else
	    printf("%d la so am",n);
	    return 0;
}
